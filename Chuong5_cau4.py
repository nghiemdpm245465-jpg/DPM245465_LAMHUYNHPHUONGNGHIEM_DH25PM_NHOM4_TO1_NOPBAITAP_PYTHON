#Câu 4: Các hàm quan trọng trong xử lý chuỗi của Python 

"""
1. len()

Trả về độ dài (số ký tự) của chuỗi.

2. str.upper() và str.lower()

Chuyển chuỗi thành chữ in hoa hoặc chữ thường.

3. str.strip(), str.lstrip(), str.rstrip()

Loại bỏ khoảng trắng hoặc ký tự khác ở đầu, cuối hoặc cả hai đầu chuỗi.

4. str.split() và str.join()

split(): tách chuỗi thành danh sách dựa trên ký tự phân cách.

join(): nối danh sách thành chuỗi với ký tự phân cách.

5. str.replace(old, new)

Thay thế ký tự hoặc chuỗi con trong chuỗi.

6. str.find(sub) và str.index(sub)

Tìm vị trí xuất hiện đầu tiên của chuỗi con.

7. str.startswith(prefix) và str.endswith(suffix)

Kiểm tra chuỗi có bắt đầu/kết thúc bằng chuỗi con hay không.

8. str.isdigit(), str.isalpha(), str.isalnum()

Kiểm tra loại ký tự trong chuỗi: số, chữ cái hay chữ + số.

9. str.format() và f-string

Chèn giá trị vào chuỗi.

10. str.capitalize() và str.title()

Viết hoa ký tự đầu tiên hoặc chữ cái đầu mỗi từ trong chuỗi.
"""